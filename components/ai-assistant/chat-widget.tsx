/**
 * AI chat widget — implemented in MILESTONE 3.
 * Will render grounded answers with inline source_url citations, and a graceful
 * "assistant is at capacity" state when the Gemini free quota is exhausted.
 * Intentionally not mounted anywhere until Milestone 3.
 */
export function ChatWidget() {
  return null;
}
