/**
 * YouTube integration — implemented in MILESTONE 2.
 * Prefer channel RSS for a light footprint; fall back to Data API free quota.
 */
export async function syncYouTube(): Promise<{ itemsSynced: number }> {
  throw new Error("Not implemented (Milestone 2)");
}
