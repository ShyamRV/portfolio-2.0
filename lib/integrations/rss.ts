/**
 * RSS integration — implemented in MILESTONE 2.
 * Dev.to / Hashnode / Medium public feeds (no auth). Normalize into content_items.
 */
export async function syncRss(): Promise<{ itemsSynced: number }> {
  throw new Error("Not implemented (Milestone 2)");
}
